The primary manuscript analyses were fixed to the PyPI package version used for the original benchmark. The revision-only local open-weight backend check used the repository state that includes OllamaBackend. The package metadata may report 0.1.1 in the local environment; therefore, the Git commit/tag should be used as the authoritative code reference for the revision-only backend check.
